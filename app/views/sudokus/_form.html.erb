<%= form_with(model: sudoku, local: true) do |form| %>
  <% if sudoku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sudoku.errors.count, "error") %> prohibited this sudoku from being saved:</h2>

      <ul>
        <% sudoku.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ncolumns %>
    <%= form.number_field :ncolumns %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
