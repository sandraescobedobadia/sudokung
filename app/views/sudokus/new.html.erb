<h1>New Sudoku</h1>

<%= form_for(@sudoku) do |form| %>
  <% if @sudoku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sudoku.errors.count, "error") %> prohibited this sudoku from being saved:</h2>

      <ul>
        <% @sudoku.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
